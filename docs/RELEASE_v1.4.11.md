# Release v1.4.11 - Исправление расчета общего выигрыша

**Дата:** 21 декабря 2025  
**Тип:** Bug Fix

## 🐛 Исправленные ошибки

### Неверный расчет общего выигрыша (Total Winnings)

**Проблема:**  
Статистика "Общий выигрыш" показывала $0, хотя у пользователя были турниры с выигрышами. Ошибка проявлялась как в веб-интерфейсе, так и в Telegram боте.

**Причина:**  
Данные из Supabase возвращаются с результатами турниров в поле `tournament_results` (массив из связанной таблицы), но расчет статистики проверял только поле `result`, которое не существует при загрузке из базы данных.

**Решение:**  
Добавлена проверка обоих полей (`result` и `tournament_results`) во всех местах, где рассчитывается статистика.

## 📁 Измененные файлы

1. **`src/app/tournaments/page.tsx`**
   - Исправлен расчет общего выигрыша в блоке статистики
   - Теперь корректно извлекает результат из `tournament_results[0]` или `result`

2. **`src/bot/commands.ts`**
   - Исправлена функция `calculateStats()` для Telegram бота
   - Добавлена проверка `tournament_results` наряду с `result`

3. **`src/app/analytics/page.tsx`**
   - Добавлен хелпер `getResult()` для унификации доступа к результатам
   - Исправлены все расчеты статистики (ITM, ROI, позиции)

4. **`src/app/results/page.tsx`**
   - Добавлен хелпер `getResult()` 
   - Исправлены расчеты totalWinnings, cashCount, finalTableCount, winsCount, avgPosition

## 🔧 Техническое решение

```typescript
// Хелпер для получения результата из правильного поля
const getResult = (tournament: any) => {
  return (
    tournament.result ||
    (Array.isArray(tournament.tournament_results)
      ? tournament.tournament_results[0]
      : tournament.tournament_results)
  );
};
```

## ✅ Тестирование

- [ ] Проверить отображение общего выигрыша на странице турниров
- [ ] Проверить статистику на странице аналитики
- [ ] Проверить статистику на странице результатов
- [ ] Проверить команду /stats в Telegram боте

## 🚀 Деплой

```bash
git add -A
git commit -m "fix: исправлен расчет общего выигрыша (tournament_results vs result)"
git push origin main
```

---

**Версия:** 1.4.11  
**Совместимость:** Полная обратная совместимость

